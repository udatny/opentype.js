export default arabicRequiredLigatures;
/**
 * Apply Arabic required ligatures to a context range
 * @param {ContextRange} range a range of tokens
 */
export function arabicRequiredLigatures(range: ContextRange): void;
